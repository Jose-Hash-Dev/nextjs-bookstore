export interface FormDataType {
  image: string;
  title: string;
  description: string;
  price: string;
  stock: string;
  pages: string;
  authorIds: number[];
  languageIds: number[];
  categoryIds: number[];
}