import styled from "styled-components";
import { DialogActions, DialogContent } from "@mui/material";

export const ActionStyle = styled(DialogActions)`
  margin-right: 20px;
`;

export const ContentStyle = styled(DialogContent)`
  width: 500px;
`;
